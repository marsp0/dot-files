sudo apt install --no-install-recommends xserver-xorg-core xserver-xorg-video-intel xserver-xorg-input-libinput x11-xserver-utils x11-xkb-utils x11-utils xinit xterm libglx-mesa0 libxfont2 xfonts-100dpi xfonts-75dpi xfonts-base xfonts-encodings xfonts-scalable xfonts-utils
sudo apt install --no-install-recommends gcc pulseaudio pavucontrol lm-sensors htop make gnupg libc6-dev libx11-dev libxft-dev libxinerama-dev libxi-dev
sudo apt install --no-install-recommends firefox-esr rofi
mkdir Documents
mkdir Downloads && cd Downloads
git clone git://git.suckless.org/dwm
git clone https://github.com/marsp0/dot-files
cp ./dot-files/dwm-config.h ./dwm/config.h
cd dwm
sudo make clean install
cd ../
cp ./dot-files/.Xresources ~/.Xresources
cp ./dot-files/.xinitrc ~/.xinitrc

Disable console printing of kernel messages
- dmesg -n 1
Removing unused locales
- ls | grep -v "en" | grep -v "locale.alias" | xargs sudo rm -rf
Remove doc folder
- sudo rm -rf /usr/share/doc/*
Make debian recognise intel graphics
- update /etc/default/grub default arg line with : i915.force_probe=4c8a -> sudo update-grub

Check space
- du -sch * .[!.]* | sort -h

Sublime Package - manual install ayu
- Download https://github.com/dempfi/ayu/releases/tag/5.0.3
- tar -xf ARCHIVE_LOCATION
- place folder as is in ~/.config/sublime-text/Packages
